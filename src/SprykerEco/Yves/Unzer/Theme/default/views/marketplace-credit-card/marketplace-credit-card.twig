{% extends model('template') %}

{% define data = {
    form: form,
} %}

{% block body %}
    {% include molecule('unzer-credit-card', 'Unzer') with {
        data: {
            publicKey: data.form.unzerMarketplaceCreditCard.public_key,
            id: data.form.unzerMarketplaceCreditCard.id,
        },
    } only %}
{% endblock %}
