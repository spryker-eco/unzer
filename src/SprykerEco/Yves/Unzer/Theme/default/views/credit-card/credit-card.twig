{% extends model('template') %}

{% define data = {
    form: form,
} %}

{% block body %}
    {% include molecule('unzer-credit-card', 'Unzer') with {
        data: {
            publicKey: data.form.unzerCreditCard.public_key,
            id: data.form.unzerCreditCard.id,
        },
    } only %}
{% endblock %}
